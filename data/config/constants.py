from kivy.metrics import dp, sp


class Constants:
    NAV_HEIGHT = dp(60)
    HEADER_HEIGHT = dp(60)
    STATUS_HEIGHT = dp(30)
    BUTTON_HEIGHT = dp(45) 
    BUTTON_WIDTH = dp(400)

    WINDOW_MIN_WIDTH= dp(1000)
    WINDOW_MIN_HEIGHT= dp(600)

